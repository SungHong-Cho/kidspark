!!!
%html
  %head
    %title Kidspark
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    
  %body
    #header_wrapper
      #header
        .logo
          = image_tag "home.png", alt: "Kidspark Home"
        %ul
          = link_to photos_index_path do
            %li{id:'gallery_tab', class:('active' if @tab_type == 'photos')}
          = link_to about_index_path do
            %li{id:'about_tab', class:('active' if @tab_type == 'about')}
          = link_to scatch_index_path do
            %li{id:'scatch_tab', class:('active' if @tab_type == 'scatch')}
          = link_to tocome_index_path do
            %li{id:'tocome_tab', class:('active' if @tab_type == 'tocome')}
          = link_to "http://blog.naver.com/orpheus123" do
            %li{id:'blog_tab'}

    #contents_wrapper 
      -if current_user
        .admin_info
          관리자로 접속되어있습니다.
          = link_to "Log Out", logout_path 
      #contents
        =yield
    #footer_wrapper   
      #footer
        = image_tag "fotter.png", alt: "2013 by 키즈파크 양재, 주소: 서울특별시 서초구 양재동 232 AT센터 3층, 전화문의: 1588-7890, 행사기간:13.07.07 ~ 13.08.25, Notice : 점핑샷 촬영부스는 양재 키즈파크에서만 운영합니다!"
        %input#last_update_time{ type: 'hidden', value: (Time.now.to_i + 3) }
